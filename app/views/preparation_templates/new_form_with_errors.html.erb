<!-- Validation failure messages -->
<% if @preparation.present? %>
  <% if @preparation.errors.any? %>
    <% @preparation.errors.full_messages.each do |message| %>
      <div class="alert alert-danger">
        <%= message %>
      </div>
    <% end %>
  <% end %>
<% end %>

<h1>
  Add a new preparation
</h1>


<hr>

<!-- Form preparation start -->
<form action="/create_preparation" method="post">
<!-- Form preparation end -->

  <!-- Label and input for name start -->
  <div class="form-group">
    <label for="name">
      Name
    </label>

    <!-- Input for name start -->
    <input type="text" id="name" name="name" class="form-control" value="<%= @preparation.name %>">
    <!-- Input for name end -->
  </div>
  <!-- Label and input for name end -->

  <button class="btn btn-block btn-outline-secondary">
    Create preparation
  </button>
</form>

<hr>

<a href="/preparations">
  Go back
</a>
